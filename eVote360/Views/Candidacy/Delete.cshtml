@model eVote360.Application.ViewModels.Candidacy.DeleteCandidacyViewModel
@{
    ViewData["Title"] = "Remove Candidacy";
}

<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header bg-warning text-dark">
                    <h4 class="mb-0">@ViewData["Title"]</h4>
                </div>
                <div class="card-body">
                    <div class="alert alert-warning">
                        <i class="bi bi-exclamation-triangle-fill"></i>
                        <strong>Are you sure you want to remove this candidacy?</strong>
                    </div>

                    <dl class="row">
                        <dt class="col-sm-4">Candidate:</dt>
                        <dd class="col-sm-8">
                            <i class="bi bi-person-fill me-2"></i>@Model.CandidateFullName
                        </dd>

                        <dt class="col-sm-4">Elective Position:</dt>
                        <dd class="col-sm-8">
                            <span class="badge bg-primary">@Model.ElectivePositionName</span>
                        </dd>
                    </dl>

                    <form asp-action="Delete" method="post">
                        <input type="hidden" asp-for="Id" />
                        
                        <div class="d-flex justify-content-between mt-4">
                            <a asp-action="Index" class="btn btn-secondary">
                                <i class="bi bi-x-circle"></i> Cancel
                            </a>
                            <button type="submit" class="btn btn-danger">
                                <i class="bi bi-check-circle"></i> Confirm
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
