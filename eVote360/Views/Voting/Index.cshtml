@model eVote360.ViewModels.Voting.DocumentInputViewModel

@{
    ViewData["Title"] = "Sistema de Votación";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0"><i class="fas fa-vote-yea"></i> Sistema de Votacion Electoral</h4>
                </div>
                <div class="card-body">
                    <div class="alert alert-info">
                        <i class="fas fa-info-circle"></i> Bienvenido al sistema de votacion electronica. 
                        Por favor ingrese su numero de cedula para comenzar.
                    </div>

                    <form asp-action="ValidateDocument" method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        
                        <div class="form-group mb-3">
                            <label asp-for="DocumentNumber" class="form-label"></label>
                            <input asp-for="DocumentNumber" 
                                   class="form-control form-control-lg" 
                                   placeholder="000-0000000-0"
                                   maxlength="13" />
                            <span asp-validation-for="DocumentNumber" class="text-danger"></span>
                            <small class="form-text text-muted">
                                Formato: 000-0000000-0 o 00000000000
                            </small>
                        </div>

                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-primary btn-lg">
                                <i class="fas fa-vote-yea"></i> Votar
                            </button>
                        </div>
                    </form>
                </div>
            </div>

            <div class="card mt-3">
                <div class="card-body">
                    <h6 class="card-title"><i class="fas fa-shield-alt"></i> Información Importante</h6>
                    <ul class="small text-muted mb-0">
                        <li>Su identidad será verificada mediante OCR de su cédula</li>
                        <li>Solo puede votar una vez por proceso electoral</li>
                        <li>Debe votar por todas las posiciones electivas</li>
                        <li>Su voto es secreto y seguro</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
