@model eVote360.Application.DTOs.Election.SaveElectionDTO
@{
    ViewData["Title"] = "Iniciar Nuevo Proceso Electivo";
    var validation = ViewBag.ValidationResult as eVote360.Application.DTOs.Election.ElectionValidationResultDTO;
}

<div class="container mt-4">
    <div class="card">
        <div class="card-header bg-primary text-white">
            <h4 class="mb-0">@ViewData["Title"]</h4>
        </div>
        <div class="card-body">
            @if (validation != null && !validation.IsValid)
            {
                <div class="alert alert-danger">
                    <h5 class="alert-heading"><i class="bi bi-exclamation-triangle"></i> Errores de ValidaciÃ³n</h5>
                    <ul class="mb-0">
                        @foreach (var message in validation.ValidationMessages)
                        {
                            <li>@message</li>
                        }
                    </ul>
                </div>
            }

            <form asp-action="Create" method="post">
                <div class="mb-3">
                    <label asp-for="Name" class="form-label">Nombre de la ElecciÃ³n <span class="text-danger">*</span></label>
                    <input asp-for="Name" class="form-control" placeholder="Ej: Elecciones Generales 2025" required />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Date" class="form-label">Fecha de RealizaciÃ³n <span class="text-danger">*</span></label>
                    <input asp-for="Date" type="date" class="form-control" required />
                    <span asp-validation-for="Date" class="text-danger"></span>
                </div>

                @if (validation != null && validation.IsValid)
                {
                    <div class="alert alert-success">
                        <i class="bi bi-check-circle"></i> Todos los requisitos se cumplen. Puede proceder a crear la elecciÃ³n.
                    </div>
                }

                <div class="d-flex justify-content-between mt-4">
                    <a asp-action="Index" class="btn btn-secondary">
                        <i class="bi bi-arrow-left"></i> Volver al Listado
                    </a>
                    <button type="submit" class="btn btn-primary" 
                            @(validation != null && !validation.IsValid ? "disabled" : "")>
                        <i class="bi bi-check-circle"></i> Crear ElecciÃ³n
                    </button>
                </div>
            </form>
        </div>
    </div>

    @if (validation != null && validation.IsValid)
    {
        <div class="card mt-3">
            <div class="card-header bg-info text-white">
                <h5 class="mb-0"><i class="bi bi-info-circle"></i> InformaciÃ³n del Proceso Electivo</h5>
            </div>
            <div class="card-body">
                <p class="mb-2">
                    <i class="bi bi-check2"></i> Se incluirÃ¡n todos los puestos electivos activos en el sistema.
                </p>
                <p class="mb-0">
                    <i class="bi bi-check2"></i> ParticiparÃ¡n todos los candidatos registrados y activos para cada puesto.
                </p>
            </div>
        </div>
    }
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
